server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/TicketBooking
    username: postgres
    password: Dharani@v11
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

